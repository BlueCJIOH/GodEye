version: '3.8'

services:
  redis:
    image: redis
    container_name: godeye-redis
    restart: on-failure
    ports:
      - "6379:6379"

  web:
    build: .
    container_name: godeye-web
    env_file:
      - ../../.env
    restart: always
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  db:
    container_name: godeye-db
    env_file:
      - ../../.env
    image: postgres:12
    restart: always
    ports:
      - "5433:5432"